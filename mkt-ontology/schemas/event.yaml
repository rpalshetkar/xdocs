# =============================================================================
# EVENT - Unified event record for all fixed income business actions
# =============================================================================
# The ONE schema. event_type drives the nested payload{} structure.
# Three-layer model: raw{} (source-native) → payload{} (canonical) → enriched{} (post-match)
# =============================================================================
blueprint: schema
kind: Event
qualifier: "ftv-"
xid: event_id|event_type
xns: [id, xid]
description: "Universal event record — all business actions flow through here"
icon: zap

# Row-level security: sales/trading users see own events only (operations/admin bypass RLS)
rls:
  read:
    - field: actor
      match: $ctx.identity

sections:
  - name: identity
    label: "Event"
    icon: zap
  - name: source
    label: "Source"
    icon: radio
  - name: raw
    label: "Raw Message"
    icon: file
  - name: payload
    label: "Payload"
    icon: package
  - name: enriched
    label: "Enriched"
    icon: layers
  - name: economics
    label: "Economics"
    icon: dollar-sign
  - name: linking
    label: "Links & Correlation"
    icon: link
  - name: lifecycle
    label: "Lifecycle"
    icon: clock
  - name: system
    label: "System"
    icon: settings

views:
  - type: table
    label: "Event Log"
    icon: table
    default: true
  - type: card
    label: "Event Cards"
    icon: layout-grid
    title: event_id
    subtitle: event_type
    badge: status
  - type: kanban
    label: "By Status"
    icon: columns
    group_by: status
    title: event_id
    subtitle: event_type
  - type: bar
    label: "By Type"
    icon: bar-chart
    x: event_type
    y: $count
  - type: pie
    label: "By Source"
    icon: pie-chart
    x: source
    y: $count

model:
  fields:
    # ── Identity ──
    event_id:      { key: true, rank: hero, fuzzy: true, grid: "3", group: identity, card: title, col: "2<", placeholder: "EVT-001" }
    event_type:    { facet: true, grid: "3", rank: hero, group: identity, card: subtitle, enum: $enums.ENUM_EVENT_TYPE, col: "1<" }
    status:        { facet: true, grid: "3", rank: primary, group: identity, card: badge, enum: $enums.ENUM_EVENT_STATUS, col: 3, default: ACTIVE }
    version:       { type: int, grid: "3", group: identity, default: 1 }

    # ── Source ──
    source:        { facet: true, grid: "3", group: source, enum: $enums.ENUM_SOURCE_TYPE, col: 4, description: "Originating system" }
    source_ref:    { grid: "3", group: source, placeholder: "MW-20260308-12345", description: "External reference ID from source system" }
    protocol:      { facet: true, grid: "3", group: source, enum: $enums.ENUM_PROTOCOL, description: "Wire protocol (FIX, FpML, SWIFT, REST, INTERNAL)" }
    actor:         { grid: "3", group: source, placeholder: "J. Smith", description: "Who created this event (user, system, service)" }
    desk:          { facet: true, grid: "3", group: source, placeholder: "FX Spot NY", description: "Trading desk / function" }
    thread_id:     { grid: "3", group: source, placeholder: "THR-A1B2C3D4E5F6", description: "Originating chat thread ID (BBG IB, Symphony)" }

    # ══════════════════════════════════════════════════════════════
    # THREE-LAYER PAYLOAD MODEL
    # ══════════════════════════════════════════════════════════════
    #
    # Layer 1: raw{}      — source-native message, immutable after capture
    # Layer 2: payload{}  — canonical transform, what the matching engine reads
    # Layer 3: enriched{} — post-match additions (risk, regulatory, settlement)
    #
    # Flow: source → raw{} → transformer → payload{} → engine → enriched{}
    # ══════════════════════════════════════════════════════════════

    # ── Layer 1: Raw (source-native, immutable) ──
    raw:
      type: dict
      grid: "12"
      group: raw
      collapsed: true
      description: "Original message as received — never mutated after capture"
      items:
        format:        { grid: "3", enum: $enums.ENUM_RAW_FORMAT, description: "FIX, FPML, SWIFT_MT, SWIFT_MX, JSON, CSV, INTERNAL" }
        version:       { grid: "3", placeholder: "4.4", description: "Protocol version (e.g., FIX 4.4, FpML 5.12, MT300)" }
        content:       { type: dict, grid: "12", collapsed: true, description: "Parsed source-native payload (or string for unparseable)" }
        raw_text:      { grid: "12", widget: textarea, description: "Original wire text (FIX tags, XML, SWIFT block)" }
        received_at:   { grid: "3", widget: datetime }
        checksum:      { grid: "3", description: "SHA-256 of raw_text for tamper detection" }
        source_msg_id: { grid: "3", placeholder: "MW-20260308-12345", description: "Message ID from source system" }

    # ── Layer 2: Payload (canonical transform) ──
    # Structure determined by event_type — see _event_payloads/*.yaml
    payload:
      type: dict
      grid: "12"
      group: payload
      description: "Canonical event-type-specific data — matching engine reads ONLY this"

    # ── Layer 3: Enriched (post-match additions) ──
    enriched:
      type: dict
      grid: "12"
      group: enriched
      collapsed: true
      description: "Post-processing additions — never present at capture time"
      items:
        risk_flags:     { type: list, grid: "12", widget: tags, description: "Risk alerts (LARGE_NOTIONAL, CONCENTRATION, etc.)" }
        regulatory:
          type: dict
          grid: "6"
          description: "Regulatory identifiers and reporting"
          items:
            uti:               { grid: "6", placeholder: "UTI202603081234..." }
            usi:               { grid: "6", placeholder: "USI202603081234..." }
            lei:               { grid: "12", placeholder: "529900T8BM49AURSDO55" }
            jurisdiction:      { type: list, grid: "6", widget: tags }
            reporting_status:  { grid: "3", enum: $enums.ENUM_REPORTING_STATUS }
            reported_at:       { grid: "3", widget: datetime }
        settlement:
          type: dict
          grid: "6"
          description: "Settlement instructions and status"
          items:
            ssi_id:            { grid: "3", placeholder: "SSI-USD-JPMC-001" }
            nostro:            { grid: "3", placeholder: "JPMC-NY" }
            value_date:        { grid: "3", widget: datepicker }
            settlement_status: { grid: "3", enum: $enums.ENUM_SETTLEMENT_STATUS }
        pricing:
          type: dict
          grid: "6"
          description: "Mid-market pricing and markup"
          items:
            mid_price:   { type: float, grid: "3" }
            spread:      { type: float, grid: "3" }
            markup_bps:  { type: float, grid: "3" }
            benchmark:   { grid: "3", placeholder: "WMR 4PM Fix" }
        compliance:
          type: dict
          grid: "6"
          description: "Compliance checks and approvals"
          items:
            approved_by:     { grid: "3" }
            limit_check:     { grid: "3" }
            wash_trade_flag: { type: bool, grid: "3" }
            best_execution:  { grid: "3" }
        enriched_at:    { grid: "3", widget: datetime }
        enriched_by:    { grid: "3", description: "Service/user that added enrichment" }

    # ── Economics (denormalized for blotter display) ──
    # Core fields populated by _make_event:
    product_type:      { facet: true, grid: "3", group: economics, enum: $enums.ENUM_PRODUCT_TYPE }
    notional:          { type: float, grid: "3", group: economics }
    ccy:               { grid: "3", group: economics, enum: $enums.ENUM_CCY }
    cpty_id:           { grid: "3", group: economics, ref: entities.name }
    # Convenience fields extracted from payload by factory (_denormalize_economics):
    direction:         { grid: "3", group: economics, description: "PAY/RECEIVE/BUY/SELL — extracted from payload" }
    ccy_pair:          { grid: "3", group: economics, description: "Currency pair (e.g. USDJPY) — FX products" }
    rate:              { type: float, grid: "3", group: economics, description: "Principal rate/price from fixed leg or trade economics" }
    tenor:             { grid: "3", group: economics, description: "Computed tenor (e.g. 5Y, 3M) from leg dates" }
    index:             { grid: "3", group: economics, description: "Float rate index (e.g. SOFR, EURIBOR)" }
    spread_bps:        { type: float, grid: "3", group: economics, description: "Spread in basis points (float leg or CDS premium)" }
    reference_entity:  { grid: "3", group: economics, description: "CDS reference entity name" }
    issuer:            { grid: "3", group: economics, description: "Bond issuer" }
    security_desc:     { grid: "3", group: economics, description: "Security description (bonds, auctions)" }
    option_type:       { grid: "3", group: economics, description: "Option type (RECEIVER/PAYER/CALL/PUT)" }

    # ── Linking ──
    links:
      type: list
      grid: "12"
      group: linking
      widget: table
      description: "References to related events"
      items:
        type: dict
        items:
          event_id:   { grid: "4", placeholder: "EVT-003", description: "Linked event ID" }
          rel:        { grid: "4", enum: $enums.ENUM_LINK_TYPE, description: "Relationship type" }
          role:       { grid: "4", description: "Role in the relationship (LHS, RHS, PARENT, CHILD)" }

    # ── Correlation / Matching ──
    correlation:
      type: dict
      grid: "12"
      group: linking
      description: "Matching engine metadata (populated when events get correlated)"
      items:
        chain_id:     { grid: "3", description: "Shared UUID linking all events in a scenario chain" }
        match_type:   { grid: "3", enum: $enums.ENUM_MATCH_TYPE }
        scenario:     { grid: "3", enum: $enums.ENUM_MATCH_SCENARIO }
        match_status: { grid: "3", enum: $enums.ENUM_MATCH_STATUS }
        cardinality:  { grid: "3", enum: $enums.ENUM_CARDINALITY }
        direction:    { grid: "3", enum: $enums.ENUM_MATCH_DIRECTION }
        actor_role:   { grid: "3", enum: $enums.ENUM_ACTOR_ROLE, description: "Functional role of the actor who produced this event" }
        breaks:
          type: list
          grid: "12"
          description: "Field-level breaks"
          items:
            type: dict
            items:
              field:     { grid: "3" }
              lhs:       { grid: "3" }
              rhs:       { grid: "3" }
              tolerance: { grid: "3" }
        resolution:
          type: dict
          grid: "12"
          description: "Resolution action taken"
          items:
            action:      { grid: "3" }
            service:     { grid: "3" }
            params:      { type: dict, grid: "3" }
            executed_at: { grid: "3", widget: datetime }
        matched_at:   { grid: "3", widget: datetime }
        matched_by:   { grid: "3" }

    # ── Lifecycle Trail ──
    transitions:
      type: list
      grid: "12"
      group: lifecycle
      widget: table
      description: "Append-only log of every state change"
      items:
        type: dict
        items:
          from_status:  { grid: "2" }
          to_status:    { grid: "2" }
          at:           { grid: "3", widget: datetime }
          by:           { grid: "2" }
          reason:       { grid: "3" }
          diff:         { type: dict, grid: "12", collapsed: true, description: "Fields that changed in this transition" }

    # ── Timestamps ──
    created_at:    { grid: "3", group: system, widget: datetime }
    updated_at:    { grid: "3", group: system, widget: datetime }
    sla_deadline:  { grid: "3", group: system, widget: datetime }
    priority:      { grid: "3", group: system, enum: $enums.ENUM_PRIORITY }

# =============================================================================
# CHAIN WORKFLOW — RFQ negotiation state machine
# =============================================================================
# Event-driven chain workflow. States are UPPERCASE, config is lowercase.
# Each transition is triggered by an event_type arriving with matching guard.
# The chain progresses through states, creating links and updating match_status.
# Compiled spec is hardcoded in apps/xftws/src/domain/rfq/chainMachine.ts
#
#   field: chain_state
#   IDLE:        → OPEN        on RFQ   (UNMATCHED)
#   OPEN:        → QUOTED      on QUOTE guard payload.status=INDICATIVE (PARTIAL)
#   QUOTED:      → NEGOTIATING on RFQ   guard payload.negotiation_status=REVISED (PARTIAL)
#                → TRADED_AWAY on RFQ   guard payload.negotiation_status=TRADED_AWAY (UNMATCHED)
#   NEGOTIATING: → QUOTED_FIRM on QUOTE guard payload.status=FIRM (PARTIAL)
#   QUOTED_FIRM: → ACCEPTED    on RFQ   guard payload.negotiation_status=ACCEPTED (MATCHED)
#                → TRADED_AWAY on RFQ   guard payload.negotiation_status=TRADED_AWAY (UNMATCHED)
#   ACCEPTED:    → TRADED      on TRADE (MATCHED)
#   scenarios: HIT → [IDLE..TRADED], MISS → [IDLE..TRADED_AWAY]
# =============================================================================
